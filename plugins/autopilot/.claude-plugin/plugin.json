{
    "name": "autopilot",
    "version": "3.42.1",
    "description": "AI 自动驾驶工程套件。包含全流程闭环（红蓝对抗 + 五层 QA + 知识工程）、AI 自适应判断 fast/standard（v3.32）、默认含 brainstorm 的交互式设计（Q&A + AskUserQuestion 审批，--fast 强制快速：跳过审批 + contract-checker + qa-reviewer，红蓝/QA 核心保留）、需求管理、智能提交（React 优化 + 代码测验 + 任务同步）、工程健康度诊断和 Worktree 自动初始化（v3.18 选择性 symlink，sessions 隔离到 worktree 分支）、契约规约协议 + contract-checker agent。(v3.38.0: QA 打分制改谓词闸门 — 验收谓词 EARS-OST + 观测绑定，三元组求值 + 强制 artifact，删整体评分/Ready to merge，封 smoke/reviewer 失败两处自审逃生口；清理 5 个孤儿 QA 文档)(v3.39.0: 谓词传动轴接线 — `## 验收场景` 成全链路谓词 SSOT，贯通红队（双消费：据 det/real 谓词写 Tier-0 硬断言）/ Tier 1.5（编排器据谓词驱动真实产物产三元组）/ 闸门 / 报告，治 B 遗留的悬空引用)(v3.40.0: 合入 Tier 1 AI-First 反过拟合减法 — plan-reviewer 去伪精度分数线、qa-reviewer 正则转语义、Explore 计数自适应、防合理化去重)(v3.40.3: stop-hook §7.6 standard design 放行——AI 结束回合等用户时改输出 systemMessage 交回用户，根治 design 阶段被自动推进绕过对齐审批)(v3.40.4: stop-hook §7.5 silent-wait 放行加 systemMessage 可见性——后台 sub-agent 等待从用户不可见改为可见提示 + 提供 /autopilot cancel 自救出路)(v3.41.0: 状态字段健壮性 — knowledge_extracted 守卫三态化（合法值放行 / 空值才回滚 merge / 非空乱值如 yes/done 自动归一 true 不回滚，治 tautological-key 白烧 iteration）+ lib.sh get_field/set_field 抗 frontmatter 重复键 + fast_mode/auto_approve 容错读，全部落在机制层、不改 SKILL)(v3.42.0: 4 个确定性硬信号根治 QA 假阳性 — ①lib.sh freshness_check（mtime 新鲜度判定 STALE/FRESH/UNKNOWN）②lib.sh lock_acceptance_tests+acceptance_tests_tampered + stop-hook §8.5.1 tamper 守卫（implement→qa 一次性检测，自门控）③Tier 5 na 可见化「⚠️ 测试有效性维度未验证」非 PASS④coverage 反向否决口径成文，全部落脚本/成文规则层，零 prompt 自检规则)(v3.42.1: set_field upsert 修复 — 键缺失时从 no-op 改为追加，根治 fast_mode→smoke 自动降级在生产单任务模式从未生效的潜伏 bug（初始 frontmatter 无 qa_scope 字段、旧 set_field 静默吞写入，知识库 [2026-05-30]）+ detect-smoke-eligible 新增生产口径回归路径 I)",
    "author": {
        "name": "String Zhao",
        "email": "stringzhao@foxmail.com"
    },
    "license": "MIT",
    "keywords": ["devops", "autopilot", "automation", "qa", "tdd", "code-review", "git", "commit", "react", "optimization", "doctor", "diagnostics", "worktree"]
}
